import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'infinite-scoll-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
